import * as mio from '../../';

export const loadingStyle = {
  icon: mio.withStyle({
    left: '50%',
    position: 'absolute',
    top: '50%',
    transform: 'translate(-50%, -50%)',
    zIndex: 1
  })
};
