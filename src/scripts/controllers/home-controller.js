'use strict';

/**
 * Represents the home controller.
 * @constructor
 * @param {Object} $scope
 * @param {SettingsService} settingsService
 */
function HomeController($scope, settingsService) {
}

module.exports = ['$scope', 'SettingsService', HomeController];
