import * as mio from '../../';

export const seriesChapterStyle = {
  listItemText: mio.withStyle({
    direction: 'rtl',
    overflow: 'hidden',
    textOverflow: 'ellipsis',
    whiteSpace: 'nowrap'
  })
};
