// Enable restricted mode.
'use strict';

// ==================================================
// Export the object.
// --------------------------------------------------
module.exports = {
    // Initialize the runtime module.
    runtime: require('./runtime')
};
